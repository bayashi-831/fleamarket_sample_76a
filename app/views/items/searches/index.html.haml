%header.pcHeader
  =render 'items/header'

%main.search.clearfix
  .l-content
    .foundMainright
      -if @items.present? 
        %h2.foundMainright__head
          =@search
          %span.foundMainright__head__text
            の検索結果
        .result
          ="1-#{@items.count}件表示"
        .itemsbox-content
          - @items.each do |item|
            %section.itemsbox
              =link_to item_path(item.id) ,class: "itemsbox__foundgoods" do
                %figure.itemsbox__foundgoods__image
                -ft_images = item.images
                =image_tag item.images[0], class: "itemsbox__foundgoods__image__itemsphoto"
                .itemsbox__body
                  %h3.found-name
                    =item.name
                  .found-details
                    .details-list
                      .found-price
                        =item.price
                        ="円"
                      .like
                        %i.far.fa-star
                          1     
      -else
        %h2.foundMain__right__resultnil
          お探しのワードの商品は存在しません

%aside.appBanner
  .inner
    %h2.inner__title
      だれでもかんたん、人生を変えるフリマアプリ
    %p.inner__text
      今すぐ無料ダウンロード
    .innner__icon        
      =link_to "#", class: "aBtn" do
        = image_tag 'icon/Download_on_the_App_Store_Badge_JP_blk_.png'
      =link_to "#", class: "gBtn" do
        = image_tag "icon/google-play-badge.png"

.home-footer
  = render 'items/top-footer'
  
=link_to "items/items/new" do
  .purchaseBtn
    %span.purchaseBtn__text
      出品する
    =image_tag 'icon/icon_camera.png' 